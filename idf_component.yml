version: "0.0.1"
description: "ALPHA Convex client for the ESP32: reactive queries, mutations, actions, and HTTP actions over the Convex sync protocol."
url: "https://github.com/get-convex/esp32"
license: "Apache-2.0"
issues: "https://github.com/get-convex/esp32/issues"
dependencies:
  idf: ">=5.0"
  espressif/esp_websocket_client: "^1.2.0"
  # ArduinoJson is required. Under the Arduino framework it comes from lib_deps;
  # for a pure ESP-IDF project add the component too:
  # bblanchon/arduinojson: "^7.0.0"
